<script setup>
// --- || import vue lib              || ------------------------------- //
import { ref } from 'vue'

// --- || import design lib           || ------------------------------ //

// --- || import Opensource lib       || ------------------------------ //

// --- || import project constants    || ------------------------------ //

// --- || import Opensource lib       || ------------------------------ //
</script>

<template>
  <div>
    <h1>비 로그인 레이아웃</h1>
    <slot />
  </div>
</template>

<style scoped>

</style>