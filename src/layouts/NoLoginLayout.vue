<script setup>
// --- || import vue lib              || ------------------------------- //
import { ref } from 'vue'

// --- || import design lib           || ------------------------------ //

// --- || import Opensource lib       || ------------------------------ //

// --- || import project constants    || ------------------------------ //

// --- || import Opensource lib       || ------------------------------ //
</script>

<template>
  <v-app>
    <v-main>
      <slot />
    </v-main>
  </v-app>
</template>

<style scoped></style>
